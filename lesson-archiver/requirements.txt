# Core
requests>=2.31.0
PyYAML>=6.0.1

# Apple Calendar (iCloud CalDAV) + published-ICS fallback
caldav>=1.3.9
icalendar>=5.0.11
recurring-ical-events>=2.1.0

# Transcript parsing
webvtt-py>=0.4.6

# Classification
anthropic>=0.40.0

# Google Drive
google-api-python-client>=2.110.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0

# Optional local ASR fallback (only needed when Zoom's transcript is missing).
# Install separately:  pip install faster-whisper
